# mf

Wire flags to makefile targets

```shell
> cat Makefile
run:
  cargo run
> mf run --flag
cargo run --flag
// runs recipe with flag 
```

## Installation

`make install`

## Usage

See `mf --help`
